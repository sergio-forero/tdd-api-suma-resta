serenity{
  project.name=adl.api
  logging = "VERBOSE"
}

serenity.include.actor.name.in.consequences = true
feature.file.encoding = iso-8859-1
enable.markdown = story narrative scenario step
environments{
  default {
    rest.base.url = "http://localhost:9095/"
  }

  stg {
    rest.base.url = "http://localhost:9095/"
  }

  dev {
    rest.base.url = "http://localhost:9095/"
  }
}
